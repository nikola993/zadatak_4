Pokreni "npm install" za instalaciju svih potrebnih paketa,
zadatak se porece komandom "npm start". Nakon pokretanja otvara se
browser na adresi http://localhost:8080/ na kojoj se nalazi
resenje zadatka 